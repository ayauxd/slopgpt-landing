# Required for chat functionality
ANTHROPIC_API_KEY=sk-ant-xxxxx

# n8n webhook URL for lead capture (primary method)
# Stores leads in Supabase + sends Slack notifications
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook

# Fallback: Email notifications for leads (via Formsubmit)
LEAD_EMAIL=fred@softworkstrading.com
